# Custom — branche `docker/dsh-nginx-proxy-3080`

Dossier centralisant **toutes les customisations** de cette branche. Chaque fichier est autonome, versionné, et pointe vers les sources.

| Fichier | Contenu |
|---|---|
| `01-docker.md` | Dockerfile multi-service, Nginx TLS, `docker-compose.yml`, volumes |
| `02-settings.md` | LAN writable par défaut, `remoteSettings`, `TRUSTED_HOSTS`, `DSH_REMOTE_SETTINGS` |
| `03-providers.md` | Providers custom (janai, pi-ai), `llm-pi-ai` dormant, Models page |
| `04-tools.md` | `tools/` : build/up/down/logs, zéro-config sur nouvelle machine |
| `05-troubleshooting.md` | Read-only, `settings are unavailable`, cache, logs |

Branche : `docker/dsh-nginx-proxy-3080` → `https://github.com/jclaudan/deepseek-harness/tree/docker/dsh-nginx-proxy-3080`
Base : `master` à `c291e796`, sync `0.1.5-rc.2`.

> Tous les chemins sont relatifs à la racine du repo. Les `file:line` permettent `Ctrl+Click`.
