# Credentials are stored here (not in .env).
# Format: version: 1, refs: { KEY_NAME: value }
# Empty values are rejected — set actual keys or remove the key entirely.
# The DEEPSEEK_API_KEY and JANAI_API_KEY can also be set in the
# launch environment (docker-compose.yml environment section).
version: 1
refs:
  # DEEPSEEK_API_KEY: "sk-..."
  # JANAI_API_KEY: "..."
